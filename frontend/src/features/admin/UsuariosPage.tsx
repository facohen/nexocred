/** Gestión de usuarios (admin). Placeholder hasta el módulo completo de M12. */
export function UsuariosPage() {
  return (
    <div className="space-y-2">
      <h1 className="text-xl font-bold text-text">Usuarios</h1>
      <p className="text-sm text-text-muted">Gestión de usuarios y roles (admin).</p>
    </div>
  );
}
